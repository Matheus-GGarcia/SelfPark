<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Como utilizar</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #f4f7fb;
      --card: #ffffff;
      --text: #0a2540;
      --muted: #5f6f86;
      --primary: #113b97;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background: linear-gradient(180deg, #eaf1ff, var(--bg));
      color: var(--text);
    }

    section.como-utilizar {
      max-width: 1200px;
      margin: 0 auto;
      padding: 80px 20px;
    }

    .header {
      margin-bottom: 48px;
    }

    .header h2 {
      font-size: 2.6rem;
      font-weight: 600;
      margin-bottom: 12px;
    }

    .header p {
      color: var(--muted);
      font-size: 1.1rem;
      max-width: 700px;
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 32px;
    }

    .card {
      position: relative;
      background: linear-gradient(160deg, rgba(4, 40, 117, 0.35), rgba(8, 51, 170, 0.35));
      backdrop-filter: blur(16px) saturate(160%);
      -webkit-backdrop-filter: blur(16px) saturate(160%);
      border-radius: 20px;
      padding: 32px 28px 28px;
      display: flex;
      flex-direction: column;
      gap: 20px;
      border: 1px solid rgba(255,255,255,0.25);
      transition: transform .35s ease, box-shadow .35s ease, border-color .35s ease;
    }
    

    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 30px 60px rgba(9, 36, 94, 0.35);
      border-color: rgba(147,197,253,0.9);
    }

    .illustration {
      width: 100%;
      height: 180px;
      border-radius: 16px;
      background: rgba(255,255,255,0.15);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 3rem;
      color: #ffffff;
    }

    .card h3 {
      font-size: 1.2rem;
      font-weight: 500;
      color: #ffffff;
    }

    .card p {
      font-size: .95rem;
      color: #dbeafe;
      line-height: 1.6;
    }

    @media (max-width: 600px) {
      .header h2 {
        font-size: 2rem;
      }
    }
    .step {
      position: absolute;
      top: 16px;
      right: 20px;
      font-size: 1.1rem;
      font-weight: 600;
      color: rgba(255,255,255,0.9);
      letter-spacing: 1px;
    }

</style>
</head>
<body>

<section class="como-utilizar">
  <div class="header">
    <h2>Como utilizar</h2>
    <p>Todas as informações necessárias para a utilização estarão disponíveis no seu e-mail, no site e também no aplicativo.</p>
  </div>

  <div class="cards">
    <div class="card">
      <span class="step">01</span>
      <div class="illustration">🚗</div>
      <h3>Dirija-se ao estacionamento</h3>
      <p>Vá até o estacionamento selecionado e, caso exista uma entrada específica para reservas, utilize-a corretamente.</p>
    </div>

    <div class="card">
      <span class="step">02</span>
      <div class="illustration">📱</div>
      <h3>Apresente o QR Code</h3>
      <p>Aproxime o QR Code da sua reserva no leitor da cancela. Não é necessário retirar o tíquete físico do estacionamento.</p>
    </div>

    <div class="card">
      <span class="step">03</span>
      <div class="illustration">🅿️</div>
      <h3>Estacione seu veículo</h3>
      <p>Após a liberação da entrada, escolha qualquer vaga disponível e estacione. As vagas não são previamente demarcadas.</p>
    </div>
  </div>
</section>

<script>
  // JS preparado para futuras interações (animações, tracking, etc.)
  console.log('Seção Como Utilizar carregada com sucesso');
</script>

</body>
</html>
